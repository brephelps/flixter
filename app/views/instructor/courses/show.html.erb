<script>
 $(function() {
  $('.lessons').sortable({
      update: function( event, ui ) {
          $.ajax({
          type: 'PUT',
          url: ui.item.data('update-url'),
          dataType: 'json',
          data: { lesson: { row_order_position: ui.item.index() } }
        });
      }
    });
  $('.sections').sortable({
      update: function( event, ui ) {
          $.ajax({
          type: 'PUT',
          url: ui.item.data('update-url'),
          dataType: 'json',
          data: { section: { row_order_position: ui.item.index() } }
        });
      }
    });
  });
</script>



<br />
<div class="booyah-box col-10 offset-1">
  <h1 class="text-center"><%= current_course.title %></h1>
  <% if current_course.image.present? %>
    <div class="image">
      <%= image_tag current_course.image.url, class: 'img-fluid' %>
    </div>
  <% end %>
  <br />
  <div class="text-center">
    <%= link_to 'Preview As Student...', course_path(current_course), class: 'btn btn-warning float-right' %>
    <br /><br />
    <!-- Button trigger modal -->
    <button type="button" class="btn btn-dark btn-lg float-right" data-toggle="modal" data-target="#newSectionModal">
      Add a new section...
    </button>
  </div>
  <br class="clr" />
  <br class="clr" />

  <ul class="sections">
    <% current_course.sections.rank(:row_order).each do |section| %>
      <li data-update-url="<%= instructor_section_path(section) %>" class="section-li">
        <div class="header">
          <span class="fa fa-ellipsis-v"></span>
          <%= section.title %>
        </div>
        <%= link_to 'New Lesson', new_instructor_section_lesson_path(section), class: 'float-right btn btn-dark btn-sm' %>
        <br class="clr" />
        <br class="clr" />

        <ul class="lessons">
          <% section.lessons.rank(:row_order).each do |lesson| %>
          <% if current_user.house == 'gryffindor' %>
            <li data-update-url="<%= instructor_lesson_path(lesson) %>" class="gryffindor">
          <% elsif current_user.house == 'slytherin' %>
            <li data-update-url="<%= instructor_lesson_path(lesson) %>" class="slytherin">
          <% elsif current_user.house == 'hufflepuff' %>
            <li data-update-url="<%= instructor_lesson_path(lesson) %>" class="hufflepuff">
          <% elsif current_user.house == 'ravenclaw' %>
            <li data-update-url="<%= instructor_lesson_path(lesson) %>" class="ravenclaw">
          <% end %>
              <div class="row">
                <div class="col-1">
                  <br />
                  <span class="fa fa-ellipsis-v"></span>
                </div>
                <div class= "col-11 text-center">
                  <p>
                    <b><%= lesson.title %></b>
                  </p>
                  <p>
                    <%= lesson.subtitle %>
                  </p>
                </div>
              </div>
              <br class="clr" />
            </li>
          <% end %>
        </ul>
        <br class="clr" />
      </li>
    <% end %>
  </ul>
</div>





<!-- Modal -->
<div class="modal fade" id="newSectionModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">×</span>
        </button>
        <h4 class="modal-title" id="newSectionModalLabel">Add a new section</h4>
      </div>
      <%= simple_form_for @section, url: instructor_course_sections_path(current_course) do |f| %>
        <div class="modal-body">
          <%= f.input :title %>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
          <%= f.submit "Add a section", class: 'btn btn-dark' %>
        </div>
      <% end %>
    </div>
  </div>
</div>